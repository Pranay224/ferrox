[package]
name = "pal"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
tun-rs = { version = "2", features = ["async"] }
tokio = { workspace = true }
thiserror = { workspace = true }

proto = { path = "../proto" }
