[package]
name = "proto"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
default = ["std"]
std = []

[dependencies]
zerocopy = { version = "0.8.47", features = ["derive"] }
thiserror = { workspace = true }
