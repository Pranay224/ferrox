[package]
name = "proto"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
default = ["std"]
std = []

[dependencies]
zerocopy = { workspace = true }
thiserror = { workspace = true }
bytes = { workspace = true }
